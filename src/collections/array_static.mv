impl array<T, N> {
    fn len(self) -> uint {
        return 0;
    }

    fn is_empty(self) -> bool {
        return false;
    }

    fn get(self, uint index) -> Option<T> {
        return 0;
    }

    fn set(self, uint index, T value) -> bool {
        return false;
    }

    fn as_ptr(self) -> T* {
        return 0;
    }
}
